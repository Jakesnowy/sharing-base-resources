# 打包指南（环境安装 + 编译 + 打包 + 上传）

> 本 mod 不能单独编译——它依赖 UE4SS 的 C++ SDK + PolyHook2（RE-UE4SS 自带），并使用
> `ue4ss.mod` 这个 xmake 规则（见 `src/xmake.lua`），所以**必须在 RE-UE4SS 源码树里编译**。
>
> 本指南面向国内网络环境，所有 GitHub 资源都走代理；MSVC 走微软 CDN（国内可直连）。
> **不需要 Node.js / CMake / Python。**

---

## 一、需要哪些环境（共 4 样）

| 环境 | 用途 | 怎么来 |
|---|---|---|
| **Git for Windows** | 拉取/提交源码 | 一般已装；脚本只检测 |
| **xmake** | 编译工具 | 脚本自动装，或手动 |
| **MSVC**（VS 2022 Build Tools +「使用 C++ 的桌面开发」工作负载） | C++ 编译器 | 脚本可经 winget 静默装；或手动（微软 CDN，国内直连） |
| **RE-UE4SS 源码** | 提供 UE4SS SDK + polyhook | 脚本经 GitHub 代理自动克隆（含子模块） |

> polyhook 本 mod 不单独下载——RE-UE4SS 自带（递归克隆就有）。

---

## 二、手动下载地址（备用，脚本一般会自动搞定）

- **Git**：https://git-scm.com/download/win
- **xmake**：https://xmake.io （页面 "Download" → Windows 安装包）
- **MSVC**：https://visualstudio.microsoft.com/visual-cpp-build-tools/
  → 下载 **"Build Tools for Visual Studio 2022"**
  → 安装界面**勾选「使用 C++ 的桌面开发」(Desktop development with C++)**
  → 安装。微软 CDN，国内直连、稳定。
- **RE-UE4SS**：`https://github.com/UE4SS-RE/RE-UE4SS`（国内直连慢/失败，**让脚本经代理克隆**即可，无需手动）

---

## 三、云端编译（推荐：本地零环境，push 即出 dll）

> 不想在本地装 xmake / MSVC / Rust？用 GitHub Actions 云端编译。
> 本地**什么都不用装**，把代码 push 到 GitHub，云端自动编译出可安装的 mod，下载即装。

### 原理

项目已自带 `.github/workflows/build.yml`。每次你 push `src/` 的改动（或在 Actions 页面手动触发），GitHub 会：
1. 在 Windows 云端克隆固定版本的 RE-UE4SS（commit `f12f0bed`，与开发环境一致）
2. 用 xmake 编译 `ModIntegratedStorageCpp`
3. 生成 `ModIntegratedStorageCpp.zip`（含 `dlls/main.dll`、`enabled.txt`、`config.txt`）

编译产物（imgui、polyhook、patternsleuth 等）会缓存复用，所以**首次约 10–15 分钟**，之后**只重编你的 `dllmain.cpp`，通常 1–2 分钟**。

### 使用步骤

1. **把改动 push 到 `main`/`master` 分支**（修改 `src/` 下的文件即可触发）：
   ```powershell
   git add src/
   git commit -m "update mod"
   git push
   ```

2. **或手动触发**：打开 GitHub 仓库 → `Actions` 标签 → 左侧选 `Build Mod` → 右侧 `Run workflow` → 选分支 → 点运行。

3. **下载产物**：进入那次运行 → 等编译结束（绿色 ✓）→ 页面最下方 `Artifacts` 区 → 下载：
   - `ModIntegratedStorageCpp.zip`（推荐，一个压缩包，下载即装）
   - 或 `ModIntegratedStorageCpp`（解压后的 mod 文件夹）

4. **安装**：解压 zip，把 `ModIntegratedStorageCpp` 文件夹复制到游戏（见下方「安装到游戏」）。

> 仓库必须是**公开仓库**（GitHub Actions 对公开仓库免费、不限时）；私有仓库会消耗免费额度。

---

## 四、本地编译（备用：本机装环境自己编译）

> 用项目自带的 `build.ps1` 脚本。它有两种模式：
> - **首次/全新机器**：加 `-Bootstrap`，自动装环境后接着编译打包。
> - **以后每次**：环境已就绪，直接运行只做编译打包。

### 1) 打开 PowerShell，进入项目目录

```powershell
cd <你放本项目的路径>   # 例如 C:\SoftWare\Project\palworld-integrated-storage-main
```

### 2) 第一次：一条命令搞定环境 + 打包

```powershell
.\build.ps1 -Bootstrap -UE4SSRoot D:\src\RE-UE4SS -Zip
```

> 把 `D:\src\RE-UE4SS` 换成你想放的路径（**建议无空格**）。
>
> 脚本会依次执行：
> 1. 检测 git（你已装）
> 2. 安装 xmake（winget；不行就经 GitHub 代理下载安装器静默装）
> 3. 经 GitHub 代理克隆 RE-UE4SS（含子模块）到 `-UE4SSRoot`
> 4. 检测/安装 MSVC（会先问 `Y/N`，**回车默认 Y**；下载几 GB，稍等；装时弹 UAC 点「是」）
> 5. 复制源码 → xmake 编译 → 组装 mod → 打 zip
>
> 若提示"无法加载脚本，未签名/执行策略"，改用：
> ```powershell
> powershell -ExecutionPolicy Bypass -File .\build.ps1 -Bootstrap -UE4SSRoot D:\src\RE-UE4SS -Zip
> ```

### 3) 以后每次：只打包

```powershell
.\build.ps1 -UE4SSRoot D:\src\RE-UE4SS -Zip
```

### 常用参数

| 参数 | 说明 |
|---|---|
| `-UE4SSRoot <路径>` | RE-UE4SS 路径（必填，或设 `$env:UE4SS_ROOT`） |
| `-Bootstrap` | 先自动准备环境再编译 |
| `-GitHubProxy <前缀>` | GitHub 代理前缀，默认 `https://gh-proxy.com/`；备选 `https://ghfast.top/`、`https://ghproxy.net/`；设 `""` 禁用 |
| `-Configuration release\|debug` | 编译配置，默认 release |
| `-Zip` | 额外生成 `ModIntegratedStorageCpp.zip`（可提交到 git 作为发布产物） |
| `-Clean` | 编译前先 `xmake clean` |
| `-NoBuild` | 与 `-Bootstrap` 配合：只装环境不编译 |

### 4) 打包产物

- 可安装 mod 文件夹：`dist\ModIntegratedStorageCpp\`
  - `enabled.txt`（空文件，存在即启用）
  - `config.txt`（配置）
  - `dlls\main.dll`（你刚编译的）
- 发布用 zip：项目根目录的 `ModIntegratedStorageCpp.zip`

---

## 五、安装到游戏（验证打包是否成功）

把 `dist\ModIntegratedStorageCpp\` 整个文件夹复制到对应目录：

- **专用服务器**：`<PalServer>\Pal\Saved\Plugins\UE4SS\Mods\`
- **客户端**：`<游戏>\Pal\Binaries\Win64\UE4SS\Mods\`

服务器和每个要用的客户端都装同一份。进游戏后，`UE4SS.log` 里出现：

```
[ISGATE] === IntegratedStorage 3.4 loaded ===
```

即说明打包 + 安装都成功。

---

## 六、把打包产物上传到 git

`.gitignore` 忽略了 `*.dll`，但**不忽略 `*.zip`**，所以最干净的做法是提交 zip：

```powershell
cd <你放本项目的路径>
git add ModIntegratedStorageCpp.zip
git commit -m "release: 打包 v3.4 mod (ModIntegratedStorageCpp.zip)"
git push
```

若想把编译出的 dll 也一起提交（被忽略了，需 `-f` 强制）：

```powershell
git add -f dist/ModIntegratedStorageCpp/dlls/main.dll
git commit -m "release: 加入编译产物 main.dll"
git push
```

> 建议提交 zip 即可（一个文件，别人下载即装）。

---

## 七、常见问题排查

| 现象 | 原因 / 解决 |
|---|---|
| 克隆 RE-UE4SS 失败 | GitHub 代理不通。换 `-GitHubProxy https://ghfast.top/` 或 `https://ghproxy.net/` 重试；或自备代理/VPN 手动克隆一次到 `-UE4SSRoot`。 |
| `xmake 配置失败` / 找不到编译器 | MSVC 没装或没勾「使用 C++ 的桌面开发」。重新运行 VS Build Tools 安装器补勾该工作负载。 |
| `xmake 编译失败: target not found` | RE-UE4SS 没自动收录本 cppmod。在 RE-UE4SS 根的 `xmake.lua` 里加一行：`includes("cppmods/ModIntegratedStorageCpp/xmake.lua")` 后重跑。 |
| `xmake` 提示要拉取依赖卡住 | 多为 GitHub 网络问题。本 mod 的 polyhook 来自 RE-UE4SS 自带 `deps`，正常无需联网；若确实缺包，临时开代理再 `-Clean` 重跑。 |
| 编译成功但找不到 dll | 脚本会在 `build/` 下递归搜索 `ModIntegratedStorageCpp.dll`；若仍找不到，手动搜一下改名为 `main.dll` 放进 `dist\ModIntegratedStorageCpp\dlls\`。 |
| 进游戏 `UE4SS.log` 没有 `[ISGATE] ... loaded` | mod 没被加载。确认 `enabled.txt` 存在（空文件即可）、UE4SS 本体已正确安装、`dlls\main.dll` 在位。 |
| PowerShell 不让运行脚本 | 用 `powershell -ExecutionPolicy Bypass -File .\build.ps1 ...`，或一次性放开：`Set-ExecutionPolicy -Scope Process Bypass`。 |
