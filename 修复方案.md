# ModIntegratedStorageCpp v3.6 — Transport Channel 修复方案

## 一、问题回顾

### 症状
- 远程客户端在游戏中周期性地出现**所有交互瘫痪**（召唤/传送/进食全部失效）
- 持续时间不等：90 秒 ~ 23 分钟
- 禁用模组后完全无此问题
- IsServer 监视器最终触发 `cached=1 fresh=0 -> full reset`，之后短暂恢复

### 隔离测试结论
| 测试 | CH RPC | injectSwap | 结果 |
|------|--------|------------|------|
| ISOLATE_INJECT_ONLY | 关闭 | 保留(但因 g_mintedSlots 为空实际未执行) | ~10分钟无故障 |
| ISOLATE_NO_SWAP | 开启 | 跳过 | ~15分钟故障复发 |

**确认：CH RPC 通道（Debug_CheatCommand 载体）是故障根因，与 injectSwap 无关。**

---

## 二、根因分析

### 当前通信架构

```
客户端                              服务器
  │                                   │
  │  Debug_CheatCommand_ToServer      │
  │  (FString ~40字节, reliable)      │
  │ ─────────────────────────────────>│ hkChRequest (post-hook)
  │                                   │   srvBuildForCamp() → ~7KB payload
  │                                   │
  │  Debug_ReceiveCheatCommand_ToClient│
  │  (FString ~7KB, reliable)         │
  │ <─────────────────────────────────│
  │                                   │
```

- 客户端每 ~3 秒发一次请求（`ISREQ|<32位hex>`）
- 服务器在 post-hook 内立即构建 ~7KB 回复并调用 `ProcessEvent` 发送
- 两个 RPC 都是 **reliable**（可靠传输），走 PlayerController 的 UChannel

### 最可能的根因：PlayerController Reliable Channel 饱和

**关键证据：故障时所有交互都失效，不只是模组功能。**

这说明问题不仅限于我们的 RPC，而是整个 PlayerController 的网络通道被卡死。

在 UE4/UE5 的网络架构中：
1. 每个 Actor 的 RPC 通过其关联的 **UChannel** 传输
2. Reliable RPC 在发出后进入 **reliable buffer**（可靠缓冲区），等待 ACK 后才移除
3. Reliable buffer 有固定大小上限（通常 256 个 packet 槽位）
4. 当缓冲区满时，新的 reliable RPC **被拒绝**，通道进入错误状态
5. PlayerController 的所有 RPC（召唤、传送、进食等）**共用同一个 UChannel**

我们的 ~7KB 回复会被拆成约 6 个 UDP 包（7KB / ~1200字节MTU）。每个包占用一个 reliable buffer 槽位。当以下条件同时满足时：
- 我们的 6 包 burst 刚好放入缓冲区
- 游戏自身的 RPC（移动同步、交互命令等）也同时到达
- ACK 延迟（服务器忙于 srvBuildForCamp 的 FindAllOf 扫描）

缓冲区瞬间填满 → 通道卡死 → 所有 PlayerController RPC 失效。

**时间不定的原因**：取决于那一刻游戏自身 RPC 流量的密度和服务器处理负载。游戏繁忙时（大量 Pal 计算、建造、战斗），我们的 burst 刚好把缓冲区顶满；空闲时不会。这完美解释了 90 秒到 23 分钟的随机间隔。

### 次要嫌疑：Debug_CheatCommand 原生处理器累积

源码注释（dllmain.cpp:546）声称 `Debug_CheatCommand_ToServer` "对非管理员惰性"，但这只是原作者的假设。原生处理器可能：
- 对非管理员调用做日志或计数
- 累积到一定次数后触发保护机制

如果这是根因，降低频率可以延缓发作但不能根治。

### 已排除的嫌疑
- **injectSwap**：ISOLATE_NO_SWAP 测试已证明无关
- **ProcessEvent 重入**：UE4 的 ProcessEvent 天生支持重入，游戏自己经常在 RPC 处理函数中再发 RPC。延迟到 on_update 没有实质区别。
- **v3.5.1 的 SEH/角色检测**：多次故障中从未触发

---

## 三、修复方案（分层）

### Layer 1（主要）：将回复 RPC 改为 Unreliable

**目标**：彻底消除 reliable buffer 饱和风险。

**原理**：
- 请求（`Debug_CheatCommand_ToServer`）只有 ~40 字节 = 1 个包，即使 reliable 也不会造成压力 → **保持 reliable**
- 回复（`Debug_ReceiveCheatCommand_ToClient`）有 ~7KB = 6 个包，是缓冲区压力的来源 → **改为 unreliable**
- Unreliable RPC 不进入 reliable buffer，丢包就丢包，客户端 3 秒后会重新请求

**实现方式**：
在模组加载时，找到 `Debug_ReceiveCheatCommand_ToClient` 的 UFunction，清除其 `FUNC_NetReliable` 标志位。

```
EFunctionFlags:
  FUNC_Net         = 0x04000000  (是网络函数)
  FUNC_NetReliable = 0x02000000  (可靠传输)
  FUNC_NetClient   = 0x00000020  (客户端执行)
```

清除 `FUNC_NetReliable` 后，该函数变为 unreliable NetClient RPC。

```cpp
// 伪代码
UFunction* replyFn = ctrl->GetFunctionByNameInChain(STR("Debug_ReceiveCheatCommand_ToClient"));
uint32_t& flags = *(uint32_t*)((uint8_t*)replyFn + OFFSET_FUNCTION_FLAGS);
flags &= ~0x02000000;  // 清除 FUNC_NetReliable
```

**风险**：
- `Debug_ReceiveCheatCommand_ToClient` 是调试函数，游戏中仅用于管理员作弊命令的回显。改为 unreliable 后偶尔丢失回显，影响可忽略。
- 请求函数不受影响，管理员作弊命令仍可靠传输。
- 需要确认 `OFFSET_FUNCTION_FLAGS`（RE-UE4SS 可能提供 `UFunction::GetFunctionFlags()/SetFunctionFlags()` API，否则用原始偏移）。

**注意事项**：
- 修改 FunctionFlags 是**全局**的，影响所有调用此函数的代码
- 如果 RE-UE4SS 提供了 API（需验证 commit c838a8ac），优先使用 API
- 如果需要原始偏移，可通过对比已知函数的 flags 来确定（在开发阶段用 ObjectDump 验证）

---

### Layer 2（加固）：Delta 同步（增量回复）

**目标**：即使 unreliable 方案有问题（如根因不是饱和而是原生处理器累积），也能大幅降低每次 RPC 的负载。

**原理**：
当前每次回复都发送完整的公会物品池（~7KB）。但大多数情况下，两次请求之间的物品池**几乎没有变化**（玩家只是在营地里建造，没有从其他营地存取物品）。

改为：
1. 服务器为每个客户端缓存上次发送的物品池快照
2. 计算当前池与上次快照的差异
3. 只发送增量：`IS2D|+Wood:50,-Stone:10,+Fiber:5,`（通常 <200 字节）
4. 客户端在本地的池上应用增量
5. 仅在以下情况发送全量：
   - 客户端首次请求（进入营地）
   - 距离上次全量超过 60 秒（强制对齐，防止增量丢失累积）
   - 增量超过全量的 50%（差异太大，不如发全量）

**预期效果**：
| 场景 | 当前 payload | Delta payload |
|------|-------------|---------------|
| 进入营地（首次） | ~7KB | ~7KB（全量） |
| 建造中（无变化） | ~7KB | ~20 字节（空增量） |
| 其他营地存取了物品 | ~7KB | ~100-300 字节 |

**数据结构**：
```cpp
// 服务器端：per-client 快照
struct ClientSnapshot {
    UObject* controller;          // 关联的 PlayerController
    std::wstring lastCampGuid;    // 上次请求的营地
    std::vector<std::pair<FName, int32_t>> lastPool;  // 上次发送的池
    uint64_t lastFullSyncMs;      // 上次全量同步的时间戳
};
static std::vector<ClientSnapshot> g_clientSnaps;
```

**增量编码格式**：
```
全量: IS1|Wood:500,Stone:300,...,         (现有格式不变)
增量: IS2|+Wood:50,-Stone:10,+Fiber:5,    (新增/变化用+，删除/归零用-)
空增量: IS2|                                 (无变化)
```

**客户端解析**：
- `IS1|` → 全量替换（现有逻辑）
- `IS2|` → 在现有 g_pool 上应用增量
- 空增量（`IS2|`）→ 不修改 g_pool

---

### Layer 3（防御）：回复延迟到 on_update

**目标**：消除任何潜在的重入风险（虽然分析认为重入不是根因，但作为防御措施成本极低）。

**原理**：
当前 `hkChRequest`（post-hook）在收到请求后**立即**调用 `ProcessEvent` 发送回复。虽然 UE4 支持重入，但改为延迟到下一帧可以：
- 确保回复不在 net driver 的 `TickDispatch`（入站处理）调用栈中发出
- 给服务器更多时间处理其他入站包
- 降低瞬时负载

**实现**：
```cpp
// hkChRequest 中不再直接发送回复
static struct {
    UObject* ctrl;
    std::wstring payload;
    bool pending;
} g_pendingReply{};

static void hkChRequest(...) {
    // ... 解析请求、找到营地、构建 payload ...
    // 不直接发送，改为暂存
    g_pendingReply.ctrl = ctrl;
    g_pendingReply.payload = srvBuildForCamp(camp);
    g_pendingReply.pending = true;
}

// on_update 中处理待发回复
static void on_update() {
    if (g_pendingReply.pending) {
        UFunction* rf = g_pendingReply.ctrl->GetFunctionByNameInChain(CH_REPLY_FN);
        if (rf) {
            struct { FString S; } rp{};
            rp.S = FString(g_pendingReply.payload.c_str());
            g_pendingReply.ctrl->ProcessEvent(rf, &rp);
        }
        g_pendingReply.pending = false;
    }
    // ... 其他 on_update 逻辑 ...
}
```

**注意**：如果同一客户端在回复未发送前又发来新请求，新请求会覆盖旧回复（只保留最新的），这是正确行为。

---

## 四、实施优先级

| 优先级 | 方案 | 改动量 | 预期效果 | 风险 |
|--------|------|--------|---------|------|
| P0 | Layer 1: 回复改 Unreliable | ~20行 | 消除 reliable buffer 饱和 | 低 |
| P1 | Layer 2: Delta 同步 | ~80行 | payload 从 7KB 降至 <300B | 中（逻辑复杂度） |
| P2 | Layer 3: 延迟回复 | ~15行 | 消除重入风险 | 极低 |

**建议**：三层同时实施。Layer 1 和 Layer 3 改动小、风险低，没有理由不做。Layer 2 改动较大但效果显著。

如果时间紧迫，可以先只做 Layer 1 + Layer 3（约 35 行改动），测试效果。如果仍有问题，再加 Layer 2。

---

## 五、验证计划

### 测试环境
- 客户端 + 服务器同机（localhost）
- 至少一个远程客户端（通过虚拟网络连接）
- 多个营地、多个公会成员

### 测试步骤
1. **基线测试（无修复）**：确认当前版本在 ~15 分钟内故障复发（已验证）
2. **Layer 1+3 测试**：部署仅含 Unreliable + 延迟回复的版本
   - 目标：连续运行 2 小时无故障
   - 监控：服务器日志 `CH reply sent` 持续输出、客户端 `CH-RECV` 持续接收
   - 如果故障仍然出现 → 根因不是 reliable buffer，需要 Layer 2 或更换传输方式
3. **全量测试（Layer 1+2+3）**：部署完整修复版本
   - 目标：连续运行 4 小时无故障
   - 额外验证：Delta 同步正常工作（日志显示 `CH delta sent len=<小数值>`）
   - 检查远程客户端的稳定性

### 回滚方案
- 所有修改通过 `#define` 控制，可快速回退到 reliable + 全量模式
- 如果 Layer 1（FunctionFlags 修改）导致问题，可通过 config.txt 开关禁用

---

## 六、备选方案（如果上述修复无效）

如果三层修复都实施后故障仍然复发，说明根因不是 reliable buffer 饱和，而是 Debug_CheatCommand 原生处理器本身有问题。此时需要完全弃用该函数。

### 备选 A：TCP Socket 直连传输
- 服务器在游戏外监听一个 TCP 端口（如 27500）
- 客户端连接到服务器 IP（localhost 或虚拟网络 IP）
- 完全绕开 UE4 网络栈
- 优点：彻底解决问题，无 RPC 限制
- 缺点：需要端口配置，虚拟网络需开放端口

### 备选 B：利用游戏自身的复制机制
- 服务器将公会物品数据写入一个已复制的属性（如 PlayerState 上的某个字段）
- 客户端通过正常的属性复制自动接收
- 优点：使用游戏原生机制，最稳定
- 缺点：需要找到合适的可复制属性，实现复杂

### 备选 C：寻找替代的 FString RPC
从 ObjectDump 中搜索到的候选：
- `PalPlayerController:UpdateCharacterNickName_ToServer(FString)` — 非调试函数，接受 FString，但有改名副作用
- `PalPlayerController:SendLog_ToClient(...)` — 结构化参数，不适合自由数据
- `PalNetworkItemComponent` 系列 — 使用复杂结构体，不适合编码自由数据

结论：在当前游戏的 RPC 函数中，`Debug_CheatCommand` 对仍然是 FString 传输的最佳选择。如果必须弃用，TCP Socket（备选 A）是最实际的替代方案。

---

## 七、技术实现备注

### FunctionFlags 偏移确认
RE-UE4SS（commit c838a8ac）可能提供以下 API：
```cpp
// 方式 1：API（优先尝试）
auto flags = replyFn->GetFunctionFlags();
replyFn->SetFunctionFlags(flags & ~EFunctionFlags::NetReliable);

// 方式 2：原始偏移（如果 API 不可用）
// UFunction 结构中 FunctionFlags 的偏移需要通过实验确定
// 可在开发阶段用 ObjectDump 对比已知 reliable/unreliable 函数来校准
```

### 增量同步的状态管理
- 客户端断线重连后，服务器端快照需要清除（防止将旧快照用于新会话）
- 世界切换（checkWorld 检测到）时清除所有快照
- 快照的 key 使用 PlayerController 指针（服务器端稳定，不会跨帧失效）

### Config 扩展
```ini
# config.txt 新增项
channel_unreliable = 1    # 0=保持reliable(调试用), 1=回复改unreliable(默认)
channel_delta = 1         # 0=全量同步, 1=增量同步(默认)
channel_full_sync_interval = 60  # 全量同步间隔(秒)
```
