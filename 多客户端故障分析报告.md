# 多客户端故障 & 对其他 Mod 影响 — 代码分析报告

> 日期：2026-08-02
> 分析文件：`src/dllmain.cpp`（v3.4，1097 行）
> 现象：单人进入服务器正常；多客户端时部分客户端共享失灵；重启服务器和客户端也不解决。

---

## 一、头号嫌疑：`hkEnterCamp` 不区分本地/远程玩家

**严重程度：极高 — 最可能的多客户端元凶**

### 问题代码

```cpp
// dllmain.cpp:838
static void hkEnterCamp(UnrealScriptFunctionCallableContext& ctx, void*) {
    (void)ctx;                    // ← 完全忽略了上下文！
    if (!isClient()) return;
    g_pool.clear();               // ← 清空池子
    g_poolDirty = true;
    g_needTrigger = true;         // ← 触发重新拉取
    ...
}
```

### 分析

`RegisterHook` 在 `PalBuilderComponent:OnEnterBaseCamp` 上的钩子，**每次这个 UFunction 被 ProcessEvent 调用时都会触发**，不分是谁的 Component。

如果 `OnEnterBaseCamp` 是 **NetMulticast**（在 Palworld 中很有可能——服务器通知所有客户端"某玩家进了营地"），那么：

```
玩家B进入营地Y → 服务器 multicast
  → 客户端A上，玩家B的 PalBuilderComponent 也触发 OnEnterBaseCamp
  → hkEnterCamp 不检查 ctx.Context 是不是本地玩家
  → 客户端A的 g_pool 被清空！
  → g_needTrigger = true → 重新拉取（12s间隔）
  → 如果多个玩家频繁进出营地 → 池子不断被清空 → 共享永远不稳定
```

### 与现象的吻合度

- ✅ 一个人玩：只有本地玩家的 enter 事件 → 池子稳定 → 正常
- ✅ 多人玩：其他玩家的 enter 事件不断清空池子 → 失灵
- ✅ "有些时候"：取决于其他玩家是否在移动
- ✅ 重启不解决：玩家重新连接后继续移动，问题立刻复现

### 验证方法

在多人时观察日志，看是否出现没有对应本地操作的 `CH enter-camp -> flagged`。

### 修复方向

在 `hkEnterCamp` 中比较 `ctx.Context` 的 Owner 是否为本地玩家：

```cpp
static void hkEnterCamp(UnrealScriptFunctionCallableContext& ctx, void*) {
    if (!isClient()) return;
    // 只响应本地玩家的营地进入事件
    UObject* comp = ctx.Context;
    if (comp) {
        UObject* owner = comp->GetOuter();  // PalBuilderComponent → owning pawn
        // 与本地 PlayerController → K2_GetPawn 比较
        // 如果不匹配 → 其他玩家的事件 → return
    }
    g_pool.clear(); g_poolDirty = true; g_needTrigger = true;
    ...
}
```

---

## 二、`FindFirstOf("PalPlayerInventoryData")` 多实例歧义

**严重程度：中高**

### 问题代码

```cpp
// dllmain.cpp:314, 335 — findCommonContainer / findDonorContainer
UObject* inv = UObjectGlobals::FindFirstOf(STR("PalPlayerInventoryData"));
```

### 分析

如果 `PalPlayerInventoryData` 被复制到其他客户端（Palworld 的组件复制行为不确定），`FindFirstOf` 可能返回远程玩家的库存数据，导致：

- `findCommonContainer()` → 找到错误玩家的公共容器
- `findDonorContainer()` → 错误玩家的容器被注入 minted slots
- 注入的槽位进了错误容器 → 本地扫描看不到 → 共享失效

### 为什么单人没问题

只有一个 `PalPlayerInventoryData` → `FindFirstOf` 总是对的。

### 修复方向

通过本地 PlayerController → Pawn → InventoryComponent 链获取，不用 `FindFirstOf`。

---

## 三、服务器 Reconcile 阻塞请求处理

**严重程度：中**

### 分析

`srvDiscoverReconcileInner()` 每 8s 在游戏线程执行：

- `FindAllOf("PalBaseCampModel")` — O(所有 UObject)
- `FindAllOf("PalItemContainer")` — O(所有 UObject)
- 交叉注册 ProcessEvent 调用 — O(据点数 × 箱子数)

多人多营地时，reconcile 可能耗时 1-3 秒。期间所有客户端的 CH 请求被排队等待。如果延迟超过 `CH_REPLY_TIMEOUT_MS`（5s），客户端会判定为未收到回复。

### 影响

不会单独导致问题，但会加剧上述问题的严重性。

### 修复方向

加 reconcile 耗时日志，监控是否超过阈值。

---

## 四、对本 Mod 是否会导致其他 Mod 失效的分析

| 风险点 | 评估 | 说明 |
|--------|------|------|
| **容器数组交换** (`injectMinted`/`restoreMinted`) | 低风险 | 临时替换 cont5 的槽数组，扫描完立即恢复。如果其他 Mod 的钩子在同一帧内读 cont5，可能看到 phantom 槽位。但交换窗口极短（一个原生函数调用），且在同一线程，实际影响极小。 |
| **`UPalUserWidget:Push` 钩子** | 安全 | 只设置 `g_needTrigger = true`，不修改参数/不阻断调用。其他 Mod 的 Push 钩子正常触发。 |
| **`Debug_CheatCommand` 钩子** | 安全 | 用 sentinel (`ISREQ|` / `IS1|`) 过滤，非本 Mod 的 cheat command 直接放行。 |
| **AOB 原生函数 detour** | 低风险 | PLH::x64Detour 链式支持。如果另一个 Mod 钩了完全相同的 collector/catalog/placement 函数，两者应能共存。但极端情况下 trampoline 冲突可能导致其中一个失效。 |
| **GC rooting minted slots** | 安全 | `SetRootSet()` 只影响本 Mod 创建的 local slot，不影响其他对象。 |
| **`OnAvailableConcreteModel_ServerInternal`** | 中风险 | 服务器端调用游戏原生函数做容器交叉注册。如果其他 Mod 也操作存储模块的模型注册，可能产生冲突。但此操作幂等且按公会隔离。 |
| **`PalUIConvertItemModel:Initialize` 钩子** | 安全 | 只设置 flag，不干预原生行为。 |

### 结论

本 Mod 对其他 Mod 的干扰风险很低。唯一需要注意的是容器数组交换——如果在交换窗口内有其他 Mod 的代码运行（理论上不太可能，因为在同一线程的同一函数调用内），可能看到临时数据。

---

## 五、修复优先级

| 优先级 | 问题 | 修复复杂度 |
|--------|------|-----------|
| **P0** | `hkEnterCamp` 不过滤远程玩家事件 | 低 — 加 `ctx.Context` Owner 比较 |
| **P1** | `FindFirstOf("PalPlayerInventoryData")` 歧义 | 中 — 改用 PlayerController 链 |
| **P2** | Reconcile 耗时监控 | 低 — 加耗时日志 |
| **P3** | `hkPush` / `hkCraftOpen` 也加本地玩家过滤 | 低 — 防御性检查 |
