# 多客户端故障 + 全局交互失灵 — 深度代码分析报告（v3）

> 日期：2026-08-02（第三版，基于帕鲁召唤失灵的关键线索重写）
> 分析文件：`src/dllmain.cpp`（v3.4，1097 行）
>
> ## 现场环境
> - 服务器：用户本地启动的幻兽帕鲁专用服务器
> - 客户端 A（用户）：连接 `127.0.0.1` 进入（本机回环）
> - 客户端 B、C（两位朋友）：通过异地组网连接虚拟 IP 进入
>
> ## 故障现象
> 1. **单人正常**：任何一人单独进服，共享功能完全正常。
> 2. **多人共享失灵**：超过 1 人进服后，游玩一段时间出现集体或部分客户端的跨营地共享失灵。重启也不解决。
> 3. **原生交互失灵（广谱）**：游玩一段时间后，**游戏自带的原生交互**出问题：
>    - 吃食物：提示成功，但物品不减少、效果不出现
>    - **召唤帕鲁**：也不灵（关键：这与物品箱/食物箱完全无关）
>    - 很多其他正常交互也有类似问题
> 4. **其他 Mod 失灵**：游玩一段时间后，其他 Mod 也停止工作。

---

## 核心结论（先行摘要）

### 关键推理链

```
帕鲁召唤失灵
  ↓
帕鲁召唤不涉及物品箱/食物箱/容器系统
  ↓
排除了"物品系统污染"作为全局失灵的唯一根因
  ↓
根因必须是影响【所有游戏系统】的东西
  ↓
唯一符合的解释：服务器游戏线程被反复阻塞 + Reliable RPC 队列饱和
```

### 两大类故障的根因

| 类别 | 根因 | 位置 |
|------|------|------|
| **多人共享失灵** | `hkEnterCamp` 不区分本地/远程玩家 → multicast 事件反复清空所有客户端池子 | `dllmain.cpp:838` |
| **全局交互失灵（含帕鲁召唤/吃食物/其他 Mod）** | **服务器游戏线程死亡螺旋**：`hkChRequest` 每次请求做 `FindAllOf`（O 全部 UObject）+ reconcile 每 8s 做 2× `FindAllOf` + 交叉注册 ProcessEvent 调用 → 游戏线程被反复阻塞 → 所有 RPC 处理延迟/超时 → 所有交互失败 | `dllmain.cpp:628`（srvCampById FindAllOf）<br>`dllmain.cpp:543-556`（reconcile FindAllOf）<br>`dllmain.cpp:693-711`（hkChRequest 全程阻塞游戏线程） |

**第二类故障（全局交互失灵）确定是本 Mod 导致的。**

### 通俗总结

> 你的服务器就像一个**只有一个员工的餐厅**。这个员工要同时做三件事：
> 1. 服务客人（处理你们的游戏操作：召唤帕鲁、吃食物等）
> 2. 每 8 秒做一次"全店盘点"（Mod 的 reconcile）
> 3. 每次被叫到就跑去仓库从头翻到尾找东西（Mod 的 `FindAllOf`）
>
> 一个人玩的时候，盘点快、被叫去翻仓库的次数少，员工还能忙得过来。
>
> 三个人玩的时候，因为 Mod 的一个 bug（分不清谁进营地），三个人的客户端**不停地**让员工去翻仓库，员工**大部分时间都在翻仓库和盘点**，根本没空服务客人——你想召唤帕鲁、想吃食物，喊了半天没人理，操作就"失败"了。

---

## 一、多人共享失灵：`hkEnterCamp` 不区分本地/远程玩家

**严重程度：极高 — 多人共享失灵的直接元凶**

### 问题代码

```cpp
// dllmain.cpp:838-843
static void hkEnterCamp(UnrealScriptFunctionCallableContext& ctx, void*) {
    (void)ctx;                    // ← 完全忽略了上下文！
    if (!isClient()) return;
    g_pool.clear();               // ← 清空池子
    g_poolDirty = true;
    g_needTrigger = true;         // ← 触发重新拉取
}
```

### 分析

`OnEnterBaseCamp` 是 **NetMulticast** 事件。任意玩家进入营地 → 服务器通知所有客户端 → 每个客户端的 `hkEnterCamp` 都触发 → 不检查是否本地玩家 → 清空池子 + 设 `g_needTrigger`。

```
玩家B进入营地Y → 服务器 multicast
  → 客户端A上，玩家B的 PalBuilderComponent 触发 OnEnterBaseCamp
  → hkEnterCamp 不检查 ctx.Context Owner → 客户端A的 g_pool 被清空
  → g_needTrigger = true → 3s 后发 RPC 重新请求
  → 3个玩家频繁移动 → 所有客户端池子不断被清空 → 共享不稳定
```

### 与现象吻合度

- ✅ 单人正常：只有本地事件
- ✅ 多人失灵：其他玩家事件清空池子
- ✅ "游玩一会后"：取决于其他玩家是否在移动
- ✅ 重启不解决：重连后继续移动，立刻复现

### 修复方向

```cpp
static void hkEnterCamp(UnrealScriptFunctionCallableContext& ctx, void*) {
    if (!isClient()) return;
    if (ctx.Context) {
        UObject* owner = ctx.Context->GetOuter();  // PalBuilderComponent → owning pawn
        if (!isLocalPawn(owner)) return;            // 远程玩家的事件 → 忽略
    }
    g_pool.clear(); g_poolDirty = true; g_needTrigger = true;
}
```

### 通俗解释

> 想象你（客户端A）在用手机查看其他营地的共享材料清单。这个 Mod 的机制是：**每次有人走进营地，就把你的清单撕掉，然后重新打电话向服务器要一份新的**。
>
> 问题在于，这个 Mod 分不清"是你自己走进了营地"还是"你的朋友走进了营地"。在 Palworld 里，**任何人**走进营地，服务器都会广播通知给所有人。所以：
>
> - 你一个人玩的时候：只有你自己走进营地才会撕清单 → 清单稳定 → 正常 ✅
> - 三个人一起玩的时候：**你朋友每走进一个营地，你的清单就被撕掉一次** → 你朋友在跑图，你的清单每隔几秒就被撕一次 → 你打开建造菜单时，共享材料要么没加载完，要么刚加载完又被撕了 → 共享失灵 ❌
>
> 修复方法很简单：**只有你自己走进营地时才撕清单，别人走进营地跟你没关系，别动你的清单。**

---

## 二、全局交互失灵（帕鲁召唤/吃食物/其他 Mod）：服务器游戏线程死亡螺旋

**严重程度：极高 — 这是导致所有原生交互失灵的根因**

### 2.1 【核心发现】`hkChRequest` 每次请求阻塞游戏线程做 `FindAllOf`

**严重程度：极高**

#### 问题代码

```cpp
// dllmain.cpp:627-632 — srvCampByIdInner，每次 CH 请求都调用
static UObject* srvCampByIdInner(const uint8_t* campGuid16) {
    std::vector<UObject*> camps; UObjectGlobals::FindAllOf(STR("PalBaseCampModel"), camps);  // ← O 全部 UObject！
    for (UObject* c : camps)
        if (c && std::memcmp((uint8_t*)c + OFF_CAMP_ID, campGuid16, 16) == 0) return c;
    return nullptr;
}

// dllmain.cpp:693-711 — hkChRequest（POST hook on Debug_CheatCommand_ToServer，运行在游戏线程）
static void hkChRequest(UnrealScriptFunctionCallableContext& ctx, void*) {
    ...
    UObject* camp = srvCampById(guid);        // ← FindAllOf！阻塞游戏线程
    std::wstring payload; srvBuildForCamp(camp, payload);  // ← 读取容器槽位
    ctrl->ProcessEvent(rf, &rp);              // ← 发送回复 RPC（再次进入 ProcessEvent）
    ...
}
```

#### 分析

`hkChRequest` 是 `Debug_CheatCommand_ToServer` 的 **POST hook**，运行在**游戏线程**上。每次客户端发来 CH 请求，服务器游戏线程要执行：

1. **`srvCampById`** → `FindAllOf("PalBaseCampModel")` — 遍历**全部 UObject**（可能数万个），逐个做类名比较
2. **`srvBuildForCamp`** — 读取所有同公会其他营地容器的槽位数据
3. **`ctrl->ProcessEvent(rf, &rp)`** — 发送回复 RPC（触发嵌套的 ProcessEvent + hook 调度）

单次耗时估计：**50-500ms**（取决于 UObject 数量和容器数量）。

#### 致命问题：`hkEnterCamp` bug 让请求频率暴增

正常情况下，CH 请求只在进出营地/打开菜单时发出（低频）。但由于 `hkEnterCamp` 的 multicast bug：

```
3个玩家在营地间移动
  → 每个玩家的 enter 事件 multicast 到所有客户端
  → 所有客户端的 hkEnterCamp 清空池子 + 设 g_needTrigger
  → 每个客户端 3s 节流后发送 Debug_CheatCommand_ToServer
  → 服务器约每 1 秒收到 1 个请求
  → 每个请求阻塞游戏线程 50-500ms
  → 游戏线程 5-50% 时间被本 Mod 占用
```

#### 通俗解释

> 想象服务器是一个只有**一个收银台**的超市（游戏线程只有一个）。正常情况下，顾客（游戏的各种操作）排队结账，每个人几毫秒就搞定，队伍流转很快。
>
> 但这个 Mod 每次收到客户端的请求时，收银员要**放下手头所有工作，跑去仓库把所有货架从头到尾数一遍**（`FindAllOf`），只为了找到一个特定营地的信息。这个"数货架"的动作可能要花几十到几百毫秒——在这段时间里，**整个超市冻结**，所有排队的人（包括你想召唤帕鲁、想吃食物的请求）都在干等。
>
> 更糟糕的是，因为前面说的"撕清单"bug，三个玩家在跑图时，客户端**每秒都在往服务器发请求**，收银员**每秒都要跑去数一遍货架**。超市有大半时间处于冻结状态。

### 2.2 Reconcile 每 8 秒做 2 次 `FindAllOf` + 大量 ProcessEvent

**严重程度：极高**

#### 问题代码

```cpp
// dllmain.cpp:543 — FindAllOf #1
std::vector<UObject*> camps; UObjectGlobals::FindAllOf(STR("PalBaseCampModel"), camps);

// dllmain.cpp:553 — FindAllOf #2
std::vector<UObject*> conts; UObjectGlobals::FindAllOf(STR("PalItemContainer"), conts);

// dllmain.cpp:566-575 — 交叉注册循环，每个都是 ProcessEvent 调用
for (...) {
    for (...) {
        for (...) {
            srvCall1(st, STR("OnAvailableConcreteModel_ServerInternal"), mo);  // ← ProcessEvent
        }
    }
}
```

#### 分析

reconcile 在游戏线程上连续执行：
- 2 次 `FindAllOf`（O 全部 UObject × 2）
- TMap 稀疏数组遍历（逐个模型的 `GetBaseCampModelBelongTo` = ProcessEvent）
- 交叉注册循环（每个 `srvCall1` = ProcessEvent）

多人多营地时，reconcile 可能连续阻塞游戏线程 **1-5 秒**。期间：
- ❌ 所有客户端的所有 RPC 排队等待
- ❌ 帕鲁 AI 不更新
- ❌ 物品系统不处理
- ❌ 网络复制停滞

#### 通俗解释

> 继续用超市的比喻：除了每秒被叫去"数货架"之外，这个 Mod 还有一个**每 8 秒自动运行一次的大盘点**（reconcile）。
>
> 大盘点的时候，收银员要：把**所有**货架数两遍（2 次 `FindAllOf`），然后还要逐个打电话通知每个营地"你们可以共享这些箱子了"（交叉注册的 `ProcessEvent` 调用）。
>
> 这个大盘点可能要花 **1-5 秒**。在这几秒里，**整个超市完全停摆**——所有人（所有玩家的所有操作）都在等大盘点结束。你在这几秒里点召唤帕鲁、点吃食物，服务器根本没空理你。

### 2.3 死亡螺旋：为什么"玩着玩着"才出现，且越来越严重

**这是最关键的机制分析。**

```
时间线：

T=0s   游戏开始，UObject 少，FindAllOf 快（<10ms），一切正常 ✓

T=30s  玩家建造了几个营地，UObject 增多
       FindAllOf ~30ms，reconcile ~500ms
       偶发轻微卡顿，玩家几乎无感

T=2min 玩家继续活动，更多帕鲁/建筑/物品
       FindAllOf ~100ms，reconcile ~1-2s
       hkEnterCamp bug 导致 CH 请求频繁
       → 游戏线程被反复阻塞
       → 部分 RPC 延迟，偶发"吃食物没反应"
       → 但重试通常能成功

T=5min 世界更复杂，死亡螺旋开始：
       ① reconcile 阻塞 2-3s
       ② 阻塞期间，客户端的 CH 请求堆积（hkEnterCamp bug 持续触发）
       ③ reconcile 结束后，服务器处理堆积的 CH 请求（每个又 FindAllOf）
       ④ 处理期间游戏线程继续阻塞
       ⑤ 下一个 reconcile 又到期了...
       → 游戏线程永远追不上 → RPC 大量超时
       → 帕鲁召唤失败、吃食物失败、其他 Mod 失灵 ✗

T=10min+ 死亡螺旋持续恶化，几乎所有交互都受影响
```

#### 通俗解释

> 为什么"一开始好好的，玩着玩着就全坏了"？因为这是一个**恶性循环**（死亡螺旋）：
>
> 刚进游戏时，地图上东西少（几个营地、几只帕鲁），服务器"数货架"很快，你感觉不到任何问题。
>
> 但随着你们建造更多营地、抓更多帕鲁、放更多箱子，地图上的东西越来越多。服务器每次"数货架"花的时间越来越长。
>
> 关键的恶性循环来了：
> 1. 服务器花 2 秒做大盘点 → 这 2 秒里啥都不干
> 2. 这 2 秒里，三个客户端又发了好几个请求（因为"撕清单"bug 没修）→ 请求积压了
> 3. 大盘点结束，服务器赶紧处理积压的请求 → 每个请求又要"数货架" → 又卡了
> 4. 还没处理完，下一个大盘点的时间又到了 → 又卡 2 秒
> 5. 请求越积越多，服务器**永远追不上** → 所有操作超时失败
>
> 这就像一个欠债越滚越多的人：刚开始只欠一点，还能周转；后来越欠越多，收入全用来还利息，本金永远还不清。**服务器的时间全被这个 Mod 占了，没时间处理正常的游戏操作。**

### 2.4 Reliable RPC 队列饱和（死亡螺旋的放大器）

**严重程度：高**

#### 分析

`Debug_CheatCommand_ToServer` 和 `Debug_ReceiveCheatCommand_ToClient` 都是 **Reliable RPC**。UE 网络中：

- 可靠 RPC 在同一连接上**排队、有序处理**
- 队列中排在本 Mod 的 RPC **后面**的所有游戏 RPC 都要等待
- 包括：**帕鲁召唤、吃食物、使用物品、建造确认**等所有游戏自带可靠 RPC

```
客户端的可靠 RPC 队列（同一连接）：
  [Mod的CH请求] → [Mod的CH请求] → [吃食物RPC] → [召唤帕鲁RPC] → ...
                                        ↑
                          排在 Mod 的 RPC 后面，必须等前面处理完
```

当服务器因死亡螺旋处理缓慢时，队列越积越长，后面的游戏 RPC 等待时间也越来越长，最终超时失败。

#### 与帕鲁召唤失灵的吻合度

- ✅ 帕鲁召唤与物品系统无关 → 排除了物品污染作为唯一根因
- ✅ 帕鲁召唤是可靠 RPC → 被队列饱和阻塞
- ✅ "提示成功但没生效" → 客户端预测显示成功，服务器端 RPC 超时未处理
- ✅ 其他 Mod 也失灵 → 其他 Mod 的 RPC 也在同一队列等待
- ✅ 渐进恶化 → 队列随时间越积越长

#### 通俗解释

> 游戏里你做的每个操作（召唤帕鲁、吃食物、放建筑），都是通过一条**排队通道**发给服务器的。这条通道就像**超市里唯一一条收银队伍**，所有人（所有操作）必须按顺序排，一个处理完才轮到下一个。
>
> 这个 Mod 把自己的请求也塞进了**同一条队伍**。问题不是 Mod 的请求"插队"——大家都排队——而是 Mod 的请求**太多了**（因为"撕清单"bug 导致每秒都在发），而且服务器处理每个请求又特别慢（要"数货架"）。
>
> 结果就是：你的"召唤帕鲁"请求排在队伍里，前面有十几个 Mod 的请求在慢慢处理。等轮到你的时候，**早就超时了**，游戏认为这个操作失败了——所以你点了召唤，画面上闪了一下像是成功了，但帕鲁实际上根本没出来。
>
> 这也解释了为什么**其他 Mod 也坏了**——其他 Mod 的请求也排在同一条队伍里，一样被堵住了。

### 2.5 交叉注册的物品系统污染（次要因素，仅影响物品相关交互）

**严重程度：中高 — 仅解释物品/食物相关的失灵，不解释帕鲁召唤失灵**

> ⚠️ 由于帕鲁召唤也失灵，此问题不再是全局失灵的**主因**，但仍是一个独立的**次要问题**，可能加重物品/食物交互的故障。

#### 食物箱被错误交叉注册

```cpp
// dllmain.cpp:456, 534 — 食物箱与储物箱被同等处理
static const wchar_t* SRV_FOOD_CLASS = L"PalMapObjectPalFoodBoxModel";
...
if (!isChest && !srvClassIs(model, SRV_FOOD_CLASS)) continue;  // ← 食物箱也交叉注册！
```

食物箱（`PalMapObjectPalFoodBoxModel`）与储物箱有**完全不同的语义**（帕鲁自动取食、食物腐败、喂食系统）。将其交叉注册到其他营地的存储模块可能破坏食物箱的正常运作，**单独**解释吃食物相关的失灵。

#### `OnAvailableConcreteModel_ServerInternal` 永久修改服务器状态

```cpp
// dllmain.cpp:573 — 每个 reconcile 累积调用，从不撤销
srvCall1(st, STR("OnAvailableConcreteModel_ServerInternal"), mo);
```

此调用**永久修改**存储模块的可用模型列表（注释明确说"we never un-register"），可能破坏服务器物品系统的容器解析逻辑。

#### 通俗解释

> 这个问题是**额外的**，只在物品/食物相关的操作上叠加影响（不能解释帕鲁召唤失灵，但能加重吃食物的问题）。
>
> 想象你有两个营地。这个 Mod 为了让你在 A 营地能用 B 营地的材料，做了一件事：**告诉 A 营地的存储系统"B 营地的箱子也是你的"**。这样你在 A 营地建东西时，游戏会自动去 B 营地的箱子里拿材料。
>
> 这对**储物箱**来说勉强合理（箱子就是存东西的）。但这个 Mod **把饲料槽（食物箱）也当成了储物箱来处理**——它告诉 A 营地"B 营地的饲料槽也是你的"。这就有问题了：
>
> - 饲料槽是帕鲁自动去吃东西的地方，有自己的运作逻辑（帕鲁走过去、吃、减少数量、食物会腐烂）
> - 现在服务器以为 A 营地也能用 B 营地的饲料槽，但 B 营地的饲料槽**物理上不在 A 营地**，帕鲁根本走不过去
> - 服务器的物品记录被搞乱了，可能导致你吃食物、帕鲁吃食物等操作"显示成功但实际没扣东西"
>
> 而且这些"告诉"是**永久性的、不可撤销的**——每 8 秒告诉一次，越积越多，服务器的记录越来越乱。

---

## 三、为什么"单人正常，多人出问题"——完整因果链

### 单人时

```
1个玩家 → hkEnterCamp 只触发本地事件 → 池子稳定
        → CH 请求低频（仅进出营地时）→ 游戏线程几乎不被阻塞
        → 营地少/容器少 → reconcile 快（<0.5s）
        → UObject 少 → FindAllOf 快（<10ms）
        → 可靠 RPC 队列空旷 → 游戏 RPC 即时处理
        → 一切正常 ✓
```

### 多人时

```
3个玩家
  ├── [共享失灵] hkEnterCamp multicast → 池子反复清空 → 共享不稳定
  │
  ├── [RPC 饱和] 每个客户端频繁发 CH 请求 → 可靠 RPC 队列拥挤
  │              → 帕鲁召唤/吃食物/其他 Mod 的 RPC 排在后面 → 超时失败
  │
  ├── [线程阻塞] hkChRequest 每次 FindAllOf → 游戏线程阻塞
  │              reconcile 每 8s 做 2× FindAllOf + 交叉注册 → 长时间阻塞
  │              → 所有 RPC 处理停滞 → 所有交互延迟
  │
  └── [死亡螺旋] 阻塞 → 请求堆积 → 处理堆积又阻塞 → 下一个 reconcile 到期
                  → 渐进恶化 → "玩着玩着全坏了"
```

### 通俗解释

> **为什么一个人玩没问题？**
>
> 一个人玩时，地图上东西少，Mod 的"盘点"和"翻仓库"都很快（几毫秒到几十毫秒），而且只有你一个人的进出营地事件，请求很少。服务器轻松应付，你感觉不到任何卡顿。
>
> **为什么三个人玩就出问题？**
>
> 1. **共享清单反复被撕**：你朋友每走进一个营地，你的共享材料清单就被清空一次，要重新向服务器要 → 共享不稳定
> 2. **服务器被堵死**：三个人在跑图，客户端不停地发请求，服务器每次处理请求都要"翻遍整个仓库"，还要每 8 秒做一次"大盘点" → 服务器忙到没空处理你的正常操作
> 3. **排队排到超时**：你的"召唤帕鲁""吃食物"等操作排在 Mod 的请求后面，等太久就超时失败了
> 4. **越玩越严重**：地图上东西越来越多，"翻仓库"越来越慢，服务器越来越追不上 → 最终几乎所有操作都失败

---

## 四、对本 Mod 是否导致其他 Mod / 原生交互失灵的评估

### 结论：确定是本 Mod 导致的

通过两个独立机制：

| 机制 | 影响范围 | 帕鲁召唤是否受影响 | 代码位置 |
|------|---------|:---:|------|
| **游戏线程死亡螺旋**（FindAllOf + reconcile 阻塞） | **所有**游戏系统 | ✅ 是 | `dllmain.cpp:628`, `543-556`, `693-711` |
| **Reliable RPC 队列饱和** | **所有**可靠 RPC | ✅ 是 | `dllmain.cpp:620-621` |
| 交叉注册 + 食物箱污染 | 仅物品/食物系统 | ❌ 否 | `dllmain.cpp:534`, `566-575` |

帕鲁召唤失灵这一现象**只可能**由前两个机制解释（影响所有系统），从而确认了本 Mod 是全局交互失灵的根因。

### 各风险点重新评估

| 风险点 | 评估 | 说明 |
|--------|------|------|
| **`hkChRequest` 中 `FindAllOf` 阻塞游戏线程** | **极高风险** | 每次 CH 请求 O(全部 UObject)，多人时每秒一次，直接拖慢服务器 |
| **Reconcile 的 2× `FindAllOf` + 交叉注册** | **极高风险** | 每 8s 连续阻塞游戏线程 1-5s，期间所有 RPC 停滞 |
| **Reliable RPC 通道饱和** | **极高风险** | 阻塞所有游戏可靠 RPC（帕鲁召唤/吃食物/建造等） |
| **`hkEnterCamp` multicast bug** | **极高风险** | 使 CH 请求频率暴增，是死亡螺旋的触发器 |
| **食物箱交叉注册** | **高风险** | 仅影响食物相关交互 |
| **`OnAvailableConcreteModel_ServerInternal` 状态污染** | **中高风险** | 仅影响物品系统，渐进累积 |
| **容器数组交换**（injectMinted/restoreMinted） | 低风险 | 仅客户端，不影响服务器 |
| **`UPalUserWidget:Push` 钩子** | 低风险 | 只设 flag，但每次 UI 更新都触发 hook 调度 |
| **AOB detour** | 低风险 | polyhook2 链式共存 |

---

## 五、修复优先级

| 优先级 | 问题 | 修复复杂度 | 预期效果 |
|--------|------|-----------|---------|
| **P0-a** | `hkEnterCamp` 加本地玩家过滤 | 低 | 修复共享失灵 + 大幅降低 CH 请求频率（缓解死亡螺旋） |
| **P0-b** | `srvCampById` 的 `FindAllOf` 改用 reconcile 缓存 | 低 — 复用 `g_instToCamp` 或新建 camp-id→camp 映射 | 消除每次 CH 请求的 O(N) 扫描，大幅降低游戏线程阻塞 |
| **P0-c** | 移除食物箱交叉注册 | 极低 — 删一行 | 修复食物相关交互 |
| **P1-a** | 降低 reconcile 频率 + 拆分耗时操作 | 中 | 减少游戏线程长阻塞（config: `reconcile_interval_ms = 30000`） |
| **P1-b** | 减少 Reliable RPC 流量（降低触发频率 / 换 Unreliable） | 中 | 缓解 RPC 队列饱和 |
| **P1-c** | reconcile 加耗时监控 + 超时熔断 | 低 | 防止单次 reconcile 阻塞过久 |
| **P2-a** | `hkPush` / `hkCraftOpen` 加本地玩家过滤 | 低 | 减少不必要触发 |
| **P2-b** | `FindFirstOf("PalPlayerInventoryData")` 改用 PlayerController 链 | 中 | 防御性 |
| **P3** | 评估 `OnAvailableConcreteModel_ServerInternal` 的替代方案 | 高 | 根本性修复物品系统污染 |

---

## 六、推荐的紧急修复（最小改动，最大收益）

### 修复 1：hkEnterCamp 加本地玩家过滤（止血共享失灵 + 降低 RPC 流量）

```cpp
// dllmain.cpp:838 — 修改后：
static void hkEnterCamp(UnrealScriptFunctionCallableContext& ctx, void*) {
    if (!isClient()) return;
    if (ctx.Context) {
        UObject* owner = ctx.Context->GetOuter();
        if (!isLocalPawn(owner)) return;  // 远程玩家的事件 → 忽略
    }
    g_pool.clear(); g_poolDirty = true; g_needTrigger = true;
}
```

### 修复 2：srvCampById 不再每次 FindAllOf（止血游戏线程阻塞）

在 reconcile 中构建 camp-id → camp 的缓存映射，srvCampById 直接查表：

```cpp
// 新增全局缓存（在 reconcile 中填充，类似 g_instToCamp）
static std::unordered_map<std::wstring, UObject*> g_campIdToCamp;

// srvCampByIdInner 改为查表：
static UObject* srvCampByIdInner(const uint8_t* campGuid16) {
    wchar_t hex[33]; hexOf(campGuid16, hex);
    auto it = g_campIdToCamp.find(hex);
    return (it != g_campIdToCamp.end()) ? it->second : nullptr;
}

// 在 srvDiscoverReconcileInner 的 (b) 循环中填充：
for (UObject* camp : camps) {
    if (!camp) continue;
    wchar_t ch[33]; hexOf((uint8_t*)camp + OFF_CAMP_ID, ch);
    freshCampId[ch] = camp;  // 加入新缓存
    ...
}
```

### 修复 3：移除食物箱交叉注册（止血食物交互失灵）

```cpp
// dllmain.cpp:534 — 修改前：
if (!isChest && !srvClassIs(model, SRV_FOOD_CLASS)) continue;

// 修改后：
if (!isChest) continue;  // 不再处理食物箱
```

### 修复 4：降低 reconcile 频率（缓解线程阻塞）

`config.txt` 中设置：
```
reconcile_interval_ms = 30000   # 从 8s 提高到 30s
```

这四项改动不需要重构架构，可以立即验证。预期效果：
- 修复 1 → 共享不再反复失灵 + CH 请求频率大幅降低
- 修复 2 → 服务器不再因每次 CH 请求阻塞
- 修复 3 → 食物相关交互恢复正常
- 修复 4 → 游戏线程阻塞频率降低 4 倍

---

## 七、验证方法

### 验证死亡螺旋（全局交互失灵根因）

1. **临时关闭本 Mod**，在同样多人环境下长时间游玩，观察帕鲁召唤/吃食物/其他 Mod 是否恢复正常。如果恢复，确认是本 Mod 的服务器负载导致。
2. **只做修复 1+2**（不关闭 Mod），观察帕鲁召唤是否恢复。如果恢复，确认是游戏线程阻塞导致（而非物品污染）。
3. 在日志中观察 `CH request sent` 和 `SRV discover` 的频率和耗时。如果 `CH request sent` 每秒多次出现，说明 hkEnterCamp bug 在放大流量。

### 验证 hkEnterCamp 问题

在多人时开启 verbose 日志，观察是否出现**没有对应本地操作**的 `CH enter-camp -> flagged`。如果玩家A站着不动但日志频繁出现，说明是远程玩家事件触发的。

### 验证食物箱污染

移除食物箱交叉注册后（修复 3），观察吃食物交互是否恢复。如果恢复，确认是食物箱交叉注册导致。

### 验证状态污染（如果修复 1-4 后仍有物品交互问题）

在长时间游玩出现故障后，**卸载本 Mod 但不重启服务器**，观察物品交互是否恢复。如果不恢复，说明服务器状态已被交叉注册永久污染，需要重启服务器清除。

---

## 八、附：`srvCampById` 性能问题详解

这是报告中新增的最重要的发现之一。代码注释（`dllmain.cpp:548-551`）提到 B2 优化已经将 `srvBuildForCamp` 中的 `FindAllOf("PalItemContainer")` 替换为 reconcile 缓存：

```
//! B2 — build instance-id -> container map ONCE per reconcile so srvBuildForCamp
//! (per client request) can resolve a chest's container directly instead of
//! FindAllOf("PalItemContainer") on EVERY request (O(all UObjects) each).
```

但 **`srvCampById`（`dllmain.cpp:628`）仍然在每次 CH 请求时做 `FindAllOf("PalBaseCampModel")`**，这是 B2 优化遗漏的同类问题。它的开销与 B2 已修复的 `FindAllOf("PalItemContainer")` 完全相同——O(全部 UObject) 每次请求。

在多人场景下（hkEnterCamp bug 导致请求频繁），这是游戏线程阻塞的主要来源之一。修复方式与 B2 相同：在 reconcile 中构建 camp-id → camp 缓存。
