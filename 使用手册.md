# IntegratedStorage 使用手册

> **跨营地建造/制作 Mod** · 适配 Palworld 1.0 · 基于 UE4SS 的 C++ Mod · 作者：Sarfflow

在你的公会里**任意营地**建造和制作时，直接使用存放在公会**其它营地**仓库里的材料。原版只能用当前营地的本地仓库；本 Mod 把同公会所有营地的仓库汇集成一个池，让每个营地都能用上整个公会的材料。

---

## 目录

1. [它做什么](#1-它做什么)
2. [版本要求与前置](#2-版本要求与前置)
3. [安装](#3-安装)
4. [配置（重点）](#4-配置重点)
5. [不同角色的配置](#5-不同角色的配置)
6. [使用方法](#6-使用方法)
7. [正常工作的标志](#7-正常工作的标志)
8. [常见问题排查](#8-常见问题排查)
9. [配置项完整参考](#9-配置项完整参考)

---

## 1. 它做什么

- **建造菜单**和**制作菜单**会显示每种材料的**真实公会汇总数量**（不只是当前营地）。
- 服务器保持权威——它从材料**实际所在位置**真实消耗，不会凭空生成。
- 确实缺少的配方仍然灰显/红显，与原版一致。
- **绝不修改**原生的 `ItemStackInfo`，因此原版**快速堆叠**和**物品回收装置**照常工作。

> 一句话：开同一个公会的所有人，在任何营地建造时，都能用到全公会所有营地的材料。

---

## 2. 版本要求与前置

- **Palworld 1.0**（Windows，x86-64）。
- **UE4SS** 已正确安装并能加载 C++ Mod（你的 `Mods/` 目录下已有其它可运行的 C++ Mod 即说明环境就绪）。
- 服务器端和**每个客户端**都要装本 Mod（同一个 DLL）。

---

## 3. 安装

把整个 `ModIntegratedStorageCpp/` 文件夹放到 UE4SS 的 Mods 目录：

```
<游戏目录>/Pal/Binaries/Win64/Mods/
└── ModIntegratedStorageCpp/
    ├── enabled.txt          （空文件——存在即启用）
    ├── config.txt           （配置文件——见第 4 节，重点）
    └── dlls/
        └── main.dll         （编译好的 Mod）
```

- **服务器和每个客户端都要装**，且文件夹内容**完全相同**（DLL 相同、config 端分别配，见第 5 节）。
- `main.dll` 需要你自行编译（见仓库 README 的「编译」章节），或从发布页下载预编译版。

---

## 4. 配置（重点）

> **v4.0 起，数据传输改用独立的 TCP 通道**，不再走游戏内的网络通道。这意味着：
> - 服务器需要**开放一个 TCP 端口**（默认 27500）。
> - 客户端需要知道**服务器的 IP 地址**。
>
> 这两步是 v4.0 必须配置的，否则客户端的公会汇总数量不会显示。

打开 `config.txt`，按你的角色填写。完整说明见[第 5 节](#5-不同角色的配置)和[第 9 节](#9-配置项完整参考)。

---

## 5. 不同角色的配置

同一个 DLL 会在运行时自动判定自己是服务器还是客户端。但 `config.txt` 里的 TCP 配置因角色而异：

### 情况 A：专用服务器（dedicated server）

```ini
external_channel   = true
external_port      = 27500
external_server_host =          # 服务器留空！它不需要连接任何人
```

**额外要做：**
- 在服务器的防火墙 / 虚拟局域网（异地组网）里**开放 TCP 27500**。
- 记下服务器在虚拟局域网里的 IP（例如 `10.0.0.5`），客户端要用。

### 情况 B：远程客户端（你和朋友连服务器）

```ini
external_channel   = true
external_port      = 27500                      # 必须和服务器一致
external_server_host = 10.0.0.5                 # 填服务器的虚拟局域网 IP
```

**说明：**
- `external_server_host` 填服务器在你们组网里的 IP，**不是** `127.0.0.1`。
- 客户端**不需要**开端口（它主动连出去）。

### 情况 C：主机单人 / 局域网主机（host）

```ini
external_channel   = true
external_port      = 27500
external_server_host =          # 留空
```

**说明：** host 同时是服务器又是本地客户端，它直接读本地容器，**不经过** TCP 通道。无需特别配置，留空即可。如果这台机器还有**远程客户端**连进来，则它同时充当服务器，需要开放 27500 端口（同情况 A）。

### 端口冲突？

如果 27500 被占用，改一个空闲端口，**服务器和所有客户端**都改成同一个值即可：
```ini
external_port = 27501
```

---

## 6. 使用方法

安装配置完成后，正常游玩即可，无需任何额外操作：

1. **加入同一个公会**——只有同公会营地的材料才会被汇总。
2. **走进任意一个公会营地**。
3. 打开**建造菜单**或**制作菜单**——你会看到每种材料的数量是**整个公会所有营地的总和**。
4. 直接建造/制作，服务器会从材料实际所在的营地真实扣除。

**触发时机**（无需手动操作，全自动）：
- 进入营地时自动获取一次。
- 打开建造/制作菜单时刷新。
- 在营地内每 ~12 秒自动刷新一次（防止数据过期）。

---

## 7. 正常工作的标志

打开 `UE4SS.log`，看到以下内容说明 Mod 正常：

**服务器端：**
```
[ISGATE] HEARTBEAT role=DEDI ...          ← 角色正确，稳定不变
[ISGATE] TCP server listening on port 27500   ← TCP 通道已启动
[ISGATE] TCP peer connected (total=2)     ← 有客户端连入
```

**客户端：**
```
[ISGATE] TCP client connected to 10.0.0.5:27500   ← 成功连上服务器
[ISGATE] CH-RECV FULL ... pool=48 ...     ← 收到公会汇总数据
[ISGATE] CH-RECV DELTA ...               ← 后续增量更新
```

如果建造菜单里的材料数量大于你当前营地仓库里的数量（包含了其它营地的），就说明 Mod 生效了。

---

## 8. 常见问题排查

### 客户端材料数量不更新 / 只显示当前营地

| 检查项 | 方法 |
|--------|------|
| `external_server_host` 是否填了服务器 IP | 客户端 config.txt 不能留空 |
| 端口是否一致 | 服务器和客户端的 `external_port` 必须相同 |
| 服务器端口是否开放 | 在服务器上确认防火墙/组网放行了 TCP 27500 |
| 客户端能否连上 | 看日志有没有 `TCP client connected to ...`；没有就是连不上（IP 错/端口没开/防火墙拦） |
| 是否在营地内 | 必须走进一个公会营地，营外不触发 |

### 服务器变成 CLIENT / 所有客户端都看不到材料

日志出现 `role=CLIENT`（本应是 `DEDI`）：
- 这是已修复的旧问题（v3.8.6+ 已根治）。若仍出现，确认你用的是 v4.0+ 的 DLL。

### 原版交互卡顿（召唤/传送/进食延迟几十秒）

- v4.0 已通过 TCP 通道**根治**此问题（不再占用游戏的可靠缓冲区）。
- 如果仍出现，确认所有端都已升级到 v4.0+，**没有**残留旧版 DLL 混用。

### 只有第一个连入的人显示正常，其他人不更新

- 这是旧版（RPC）的多客户端 bug，v4.0 已修复（每个 TCP 连接独立分发）。
- 确认所有人都用 v4.0+ 的 DLL。

### 换图 / 退出据点后材料数不显示

- 正常现象，重新走进一个公会营地即可触发刷新。

### 快速堆叠 / 物品回收装置失效

- **不会发生**。本 Mod 绝不修改原生 `ItemStackInfo`，这些功能完全不受影响。如果失效，检查是否装了其它冲突的 Mod。

---

## 9. 配置项完整参考

所有键都**可选**，缺失时使用内置默认值。`#` 或 `;` 开头的行是注释。

| 键 | 默认值 | 适用角色 | 含义 |
|----|--------|---------|------|
| `verbose` | `true` | 全部 | 是否在 `UE4SS.log` 输出详细 `[ISGATE]` 诊断。功能正常后可设 `false` 让日志安静。 |
| `reconcile_interval_ms` | `8000` | 服务器/主机 | 重新扫描公会箱子并重新应用跨营地合并的频率（毫秒）。最小 500。 |
| `isi_refresh_ms` | `1500` | 全部 | （保留/兼容旧配置）无需修改。最小 200。 |
| `external_channel` | `true` | 全部 | TCP 通道总开关。保持开启。 |
| `external_port` | `27500` | 全部 | TCP 端口。服务器监听此端口，客户端连接此端口。**两端必须一致**。范围 1–65535。 |
| `external_server_host` | *（空）* | 仅客户端 | 服务器的 IP/主机名。**服务器/主机留空**；客户端填服务器 IP。 |
| `channel_delta` | `true` | 服务器 | 增量同步（只发送变化项），大幅降低流量。保持开启。 |
| `channel_full_sync_interval` | `3600000` | 服务器 | 增量模式下强制全量重发的间隔（毫秒），作为自愈兜底。默认 1 小时。最小 5000。 |

---

## 备注

- 本 Mod 的核心价值（服务器跨营地消耗）**不依赖 TCP 通道**——服务器通过容器交叉注册让原生建造流程直接读取真实容器。TCP 通道只服务于**远程客户端的显示**。因此即使通道暂时连不上，**建造消耗仍然是正确的**，只是客户端显示的数量可能不准。
- 如需阅读源码或自行编译，见仓库根目录的 `README.md`。
