# 故障分析报告 — 客户端 reply 接收链路失效 + 服务器停发 CH 应答（v3.8.1）

> 日期：2026-08-04
> 分析版本：v3.8.1（HEAD: `767520c`）
> 日志来源（`AGENT.md` 定义的真实路径，非项目内副本）：
> - 服务器：`D:\Game\Steam\steamapps\common\PalServer\Pal\Binaries\Win64\ue4ss\UE4SS.log`
> - 客户端：`D:\Game\Steam\steamapps\common\Palworld\Pal\Binaries\Win64\ue4ss\UE4SS.log`
> 日志快照时段：2026-08-04 10:04:17 – 10:20:11
> 说明：本文行号基于 10:20 日志快照，日志增长后行号会偏移。

---

## 一、故障现象

1. **共享交互失灵**：客户端进服后，跨营地物品共享完全不工作，容器内容长期不刷新。
2. **客户端持续重试无果**：客户端不断发送 CH 请求，但 `hookFires` 计数**自始至终为 0**，即从未收到过任何一个服务器应答。
3. **服务器侧先正常后停发**：服务器在 10:04–10:13 期间正常构建并发送应答（`CH reply sent`），但 **10:13:23 之后完全停止**构建/发送，只剩 `SRV discover` 心跳。
4. **角色判定正常**：两端 ROLE 检测均正确（服务器 `DEDI`、客户端 `CLIENT`），**不是**之前 watchdog 误判那一类问题。

> 与历史故障的区别（重要）
> - v3.4 多人共享失灵（`多客户端故障分析报告.md`）：`hkEnterCamp` multicast + `FindAllOf` 死亡螺旋 → 已修。
> - v3.6 reliable buffer 饱和（`修复方案.md`）：reply 能发出但 PlayerController UChannel 被顶死 → 已通过 Layer 1（unreliable）+ Layer 2（delta）+ Layer 3（deferred）修复。
> - watchdog 角色误判（项目内 2026-08-03 20:37 旧副本）：`server=0 dedicated=1 -> CLIENT` → 已由 `767520c`（v3.8.1）修复，本次真实日志未复现。
> - **本次是以上修复全部落地后新暴露的问题**：reply 通道在客户端侧根本不被触发。

---

## 二、关键证据（真实日志）

### 2.1 客户端 reply hook 从未触发（核心症状）

客户端日志 `UE4SS.log`：

```
1915  [10:03:12] [ISGATE] core channel hooks OK (request=Debug_CheatCommand reply=Debug_ReceiveCheatCommand + enter/exit)
2015  [10:04:17] [ISGATE] ROLE server=0 dedicated=0 -> CLIENT (display)
2020  [10:04:22] [ISGATE] CH request sent camp=c9fb1fa86877aa45ab14e5cd23c0bcd2 myCalls=1 hookFires=0
...
2279  [10:20:11] [ISGATE] CH request sent camp=c9fb1fa86877aa45ab14e5cd23c0bcd2 myCalls=154 hookFires=0
```

- 154 次请求，`hookFires` **恒为 0**。
- `hookFires` 对应挂在 `Debug_ReceiveCheatCommand`（服务器→客户端 reply 通道）上的接收计数。恒为 0 ⇒ **客户端从未收到任何一个服务器应答**。

### 2.2 从未成功 fetch 过一次

```
2028  [10:04:47] HEARTBEAT role=CLIENT pool=149 calls=4 awaiting=0 miss=0 inCamp=1 sinceFetch=7110ms
2187  [10:13:47] HEARTBEAT role=CLIENT pool=149 calls=77 awaiting=1 miss=2 inCamp=1 sinceFetch=24688ms
2274  [10:19:47] HEARTBEAT role=CLIENT pool=149 calls=149 awaiting=1 miss=74 inCamp=1 sinceFetch=384891ms
```

- `sinceFetch` 从 7110ms 单调增长到 384891ms（约 6.4 分钟）⇒ **从未归零**，即从未成功拉取过数据。
- `pool=149` 恒定 ⇒ 149 个容器永远等不到数据。
- `miss` 从 2 累加到 74，`awaiting=1` ⇒ 请求持续超时。

```
2188  [10:13:50] [ISGATE] CH WARNING: 3 consecutive replies missed (last trig 5016ms ago) — server not responding to this client's camp requests
```

### 2.3 服务器：先正常应答，后中途停发

服务器日志 `UE4SS.log`：

```
1998  [10:04:17] [ISGATE] ROLE server=1 dedicated=1 -> DEDICATED (server)
2002  [10:04:22] [ISGATE] CH build: otherCamp-conts=33 items=149 len=2974
2003  [10:04:22] [ISGATE] L2: FULL sync len=2974
2004  [10:04:22] [ISGATE] CH reply sent len=2974
...（10:04–10:13 期间持续 build + reply sent）...
2306  [10:13:23] [ISGATE] CH reply sent len=29          ← 最后一次 CH reply sent
2307  [10:13:29] [ISGATE] SRV discover: chests=50 camps=5 guilds=1 inst=50 campIds=5
...（此后直到 10:20:09 日志结尾，只剩 SRV discover，再无任何 CH build / CH reply sent）...
```

- 10:13:23 之后服务器**只 discover、不 build/reply**。
- 注意：即使在 10:04–10:13 服务器正常发送应答的时段，客户端 `hookFires` 也仍是 0 ⇒ **应答从未送达客户端接收函数**。

---

## 三、根因分析（候选，按可能性排序）

> 现象已确认，根因尚未最终定位。以下为基于日志与 commit 历史的推断，需用第六节诊断手段验证。

### 候选 A（最可能）：v3.8 deferred reply 改造导致 reply 投递链路在客户端侧断裂

相关 commit：
- `3433da2` fix: defer CH reply to on_update to eliminate net driver re-entrancy (v3.8)
- `ec67b1e` fix: role-watchdog requires 3x confirmation + deferred reply RF_PendingKill guard (v3.8.1)

推断链：
```
v3.8 把 reply 从 hkChRequest(post-hook) 内同步发送，改为 on_update 延迟发送
  + v3.8.1 给延迟 reply 加了 RF_PendingKill 守卫
  ↓
服务器端“CH reply sent” 日志仍能打印（说明发送代码路径执行了）
  ↓
但客户端 Debug_ReceiveCheatCommand hook 全程不触发（hookFires=0）
  ↓
可能：延迟发送时使用的目标 PlayerController / UFunction 在发送时机已失效或被守卫误拦，
      或 deferred 队列里的请求在服务器侧被丢弃（对应 10:13:23 后停发）
```

吻合度：
- ✅ `hookFires=0` 从会话第 1 秒就是 0（链路从一开始就不通）
- ✅ 服务器 10:13:23 停发，时间点接近客户端开始 miss（10:13:47）——疑似 deferred 队列/守卫在某条件下停止产出 reply

### 候选 B：camp id 不匹配，服务器应答内容对不上客户端请求

- 客户端请求固定 `camp=c9fb1fa86877aa45ab14e5cd23c0bcd2`，`inCamp=1`（玩家所在营地）。
- 服务器 `SRV discover: camps=5`，但一直在 `CH build: otherCamp-conts=33`（构建的是“其他营地”）。
- 若服务器始终未把该客户端请求的 camp 当作有效目标，或应答的 camp 与请求方不匹配，客户端可能丢弃。

> 注意：候选 B 不能解释 `hookFires=0`（包若到达客户端接收函数，hook 必触发，与内容匹配无关）。故 B 即便成立，也只是次要因素，主因仍在传输/投递层（候选 A）。

### 候选 C：服务器侧 CH build 触发源在 10:13:23 后失效

服务器 10:13:23 后只剩 `SRV discover`，说明“构建应答”的触发条件不再满足。可能是：
- 触发 CH build 的 hook 不再被调用；
- 服务器认为无活跃客户端请求（但客户端请求一直在发，矛盾点需查）；
- deferred 队列被某守卫清空后未恢复。

这是**独立于客户端问题的二次故障**，需单独定位，但发生时客户端本来就已经收不到。

### 已排除
- **watchdog 角色误判**：本次两端 ROLE 均正确，未复现。
- **reliable buffer 饱和**：v3.6 Layer 1 已将 reply 改 unreliable，且本次 `hookFires=0` 是“根本没收到”而非“通道被顶死”，症状不同。

---

## 四、通俗解释

> v3.6 之前的问题是“服务器应答太重，把唯一一条可靠通道堵死了”——应答其实发出去了，只是堵在路上。
>
> 这次的问题完全相反：**服务器喊了，客户端的耳朵（`Debug_ReceiveCheatCommand` 这个接收钩子）根本没响过**。从进游戏第一秒起，`hookFires` 就一直是 0——服务器无论发不发、发什么，客户端都像没装接收器一样。
>
> 更糟的是服务器在 10:13 之后连“喊”都停了（不再 build/reply），只剩每 8 秒一次的“我还在”（`SRV discover`）。于是一个在喊、一个听不见，后来喊的那个也不喊了——交互自然彻底失灵。
>
> 最可疑的是 v3.8 那次“把应答改成延迟发送”的改动（为了让服务器不卡）。改动本意是好的，但很可能让应答在“延迟发送”这一步从客户端的接收路径上消失了。

---

## 五、推荐解决方案

### 第 0 步（必须先做）：加诊断日志定位根因

在动任何修复前，先用两条日志把候选 A / B / C 区分开。成本极低，能避免盲改。

**客户端** — `Debug_ReceiveCheatCommand` hook 入口第一行加无条件日志：
```cpp
// hkChReply 入口
g_hookFires++;  // 现有计数
// 新增：无论后续校验如何，先打印“包到了”
LOG("[ISGATE] CH-RECV raw len=%zu (hookFires=%d)", payloadLen, g_hookFires);
```
- 若该日志**从不出现** ⇒ 包根本没到客户端函数 ⇒ 传输/投递层问题（候选 A）。
- 若该日志**出现但 g_pool 不更新** ⇒ 内容/解析问题（候选 B）。

**服务器** — `CH reply sent` 处打印目标与 camp：
```cpp
// 发送 reply 前
LOG("[ISGATE] CH reply PREPARE ctrl=%p camp=%s reqCamp=%s match=%d pendingKill=%d",
    ctrl, buildCampIdHex, reqCampIdHex, (buildCampId==reqCampId),
    ctrl->IsPendingKill());
```
- 核对 `c9fb1fa8…` 是否在服务器侧被识别；
- 核对 deferred 发送时 `ctrl` 是否已 `RF_PendingKill` 被守卫误拦（候选 A 的关键）。

### P0：最小修复（确认候选 A 后）

若诊断确认是 deferred reply 链路问题：
1. 检查 `ec67b1e` 的 `RF_PendingKill` 守卫是否**误杀**了仍有效的 PlayerController（宽化条件或改用更稳健的有效性判断）。
2. 确认 deferred 队列在 10:13:23 这类时间点是否被异常清空（对应服务器停发）。
3. 若 deferred 方案弊大于利，评估回退到 v3.8 之前的“hook 内同步发送”，但保留 unreliable + delta（v3.6 的 Layer 1/2）以避免重蹈 reliable buffer 覆辙。

### P1：候选 B 的修正（若 id 不匹配）

- 在服务器 `SRV discover` 时打印每个 camp 的 id，核对 `c9fb1fa8…` 是否在 5 个 camp 内。
- 若客户端请求的 camp 在服务器侧查不到，排查客户端 `inCamp` 判定与 camp id 取值逻辑。

### P2：架构备选 —— 独立端口传输（TCP Socket 直连）

> 此方案已在 `修复方案.md` 第六节列为「备选 A」。针对本次“reply 通道完全不通”，它看似能绕过问题，**但无法“完美”解决**，需谨慎评估。

**能解决的**
- 绕开 `Debug_ReceiveCheatCommand` 这个失效的 reply 通道；数据可从服务器进程直接流到客户端进程。

**不能解决的（为什么不完美）**
1. **两端 UE 集成仍在**：数据源是服务器内存里的 `UPalItemContainer`，目的地是客户端同一批对象。换通道只换了中间传输段，读写两端的 hook 与对象生命周期问题原封不动。
2. **跨线程新风险**：socket 收数据在独立线程，UE 非线程安全，必须排队到游戏 Tick 再 apply；而游戏 RPC 天然在游戏线程触发。换 socket 反而新增 access violation / 崩溃面。
3. **服务器停发未定位**：若根因在服务器“构建数据”端（候选 C），换通道无效——服务器没数据可发。
4. **部署成本**：dedicated server 额外端口的防火墙/托管面板放行、多客户端路由、断线重连、防伪造/鉴权，这些用游戏 RPC 时由 Steam/UE 兜底。
5. **id 不匹配照样不匹配**：候选 B 是数据/逻辑层问题，换传输层无效。

**结论**：独立端口应作为「确认游戏 RPC 通道无法修复」之后的兜底，而**不是**排查第一步。当前应先用第 0 步诊断确认根因；只有证明 `Debug_ReceiveCheatCommand` 通道在本游戏版本上确实无法稳定工作时，才启动 P2。

---

## 六、验证方法

1. **部署带第 0 步诊断日志的版本**，复现故障。
2. 看客户端是否出现 `CH-RECV raw`：
   - 不出现 ⇒ 锁定候选 A（传输/投递层），走 P0。
   - 出现但 pool 不更新 ⇒ 锁定候选 B，走 P1。
3. 看服务器 `CH reply PREPARE`：
   - 10:13:23 后是否停止打印 ⇒ 锁定候选 C（服务器停发的触发条件）。
   - `pendingKill=1` 频繁出现 ⇒ 锁定 `RF_PendingKill` 守卫误杀（P0-1）。
4. 修复后目标：连续运行 ≥2 小时，客户端 `hookFires` 随请求增长、`sinceFetch` 周期性归零、无 `CH WARNING`。

---

## 七、结论

- 本次故障**不是**历史已修复问题（死亡螺旋 / reliable buffer 饱和 / watchdog 误判）的复发，而是 v3.8/v3.8.1 deferred reply 改造后新暴露的 **reply 接收链路失效**，叠加一个**服务器中途停发**的二次故障。
- 头号症状客观可量化：客户端 `hookFires` 恒为 0、`sinceFetch` 从未归零。
- **不建议**在未定位根因前直接切换到独立端口传输（P2）；应先用诊断日志（第 0 步）区分候选 A/B/C，再做针对性最小修复。
